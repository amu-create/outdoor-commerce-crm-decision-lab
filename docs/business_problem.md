# Business questions and current answerability

분석 기준일은 `2026-08-07`, 분석 윈도우는 `2023-08-01~2026-08-07`이다. 현재 산출물은 주문 `96,277건`, 고객 `66,162명`, 첫 구매 코호트 `37개`를 사용한다.

| 비즈니스 질문 | 대응 데이터 | 현재 답변 가능 여부 | 현재 근거 또는 불가 사유 | 다음 단계 |
|---|---|---|---|---|
| 월별 순매출 증가는 주문수와 AOV 중 무엇이 주도하는가? | `orders`, `order_items`, `products` | 가능 | 2026-07 순매출 `30.1%`, 주문수 `29.6%`, AOV `0.4%` 증가로 주문량 영향이 더 큼 | 부분월 2026-08을 제외한 완전월 추세 유지 |
| 첫 구매 코호트별 M+1~M+12 재구매 차이는 무엇인가? | `orders` | 부분 가능 | `37개` 코호트와 `444셀`은 계산되지만 M+1 중앙값 `2.3%`, M+12 `1.8%`로 감쇠가 약하고 `90셀`은 절단됨 | 절대 타이밍 대신 코호트 간 상대 비교만 사용 |
| 취소·반품을 제외한 순매출과 상품 원가 마진은 얼마인가? | `orders`, `order_items`, `products` | 가능 | 순매출 `6,248,326`, 총매출 `8,324,326`; 마진은 `sale_price-cost`로 계산됨 | 채널·카테고리별 마진 분해 |
| 유입채널과 퍼널 단계별 이탈은 어디에서 발생하는가? | `users.traffic_source`, `events.event_type` | 불가 | 유입채널 `5개`, 이벤트 유형 `6개`는 존재하지만 현재 집계 SQL과 결과 파일이 없음 | 2026-08-12까지 채널·퍼널 SQL 작성 |
| 어떤 고객군을 CRM 액션 대상으로 우선해야 하는가? | 주문·상품·채널·반품 이력 | 불가 | 세그먼트 규칙과 대상 고객 산출물이 아직 없음. 할인 품목도 `0/180,066개`라 할인 민감군은 측정 불가 | 재구매·마진·카테고리·반품 기준 세그먼트 산출 |

현재 답변 가능한 질문은 `2개`, 부분 가능한 질문은 `1개`, 현재 불가능한 질문은 `2개`다. 불가능 항목은 예정 결과를 완료된 성과로 표시하지 않는다.
